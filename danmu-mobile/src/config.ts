const title = "Lemon";
const url = "http://localhost:9000/";
const desktop = "/";
const icp = "icp备案信息";
const security = "公网安备信息"


const config = {
    title: title,
    url: url,
    desktop: desktop,
    icp: icp,
    security: security,
}

export default config;